{
  "name": "@clinic/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Postgres schema, RLS policies and tenant-isolation test suite for the clinic platform (Supabase).",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint config for @clinic/db yet'"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
