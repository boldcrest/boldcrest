{
  "name": "@clinic/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure domain logic for the clinic platform: follow-up protocols and WhatsApp message building. No React, no DOM, no localStorage.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "date-fns": "^4.4.0"
  },
  "devDependencies": {
    "eslint": "^9",
    "eslint-config-next": "16.3.5",
    "typescript": "^5",
    "vitest": "^5.0.1"
  }
}
